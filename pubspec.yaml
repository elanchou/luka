name: vault_app
description: A secure vault application for managing secrets and seed phrases
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  google_fonts: ^6.1.0
  flutter_secure_storage: ^10.0.0
  encrypt: ^5.0.3
  provider: ^6.1.5+1
  shared_preferences: ^2.5.4
  path_provider: ^2.1.5
  uuid: ^4.5.2
  intl: ^0.20.2
  local_auth: ^3.0.0
  share_plus: ^12.0.1
  bip39: ^1.0.6
  crypto: ^3.0.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
